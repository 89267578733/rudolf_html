{
  "name": "toilet-tasks-rudolf",
  "version": "1.0.0",
  "description": "A 2D browser game where Rudolf solves IT tasks in toilet cubicles",
  "main": "index.js",
  "scripts": {
    "start": "npm run dev",
    "build": "vite build",
    "preview": "vite preview",
    "dev": "vite",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "dependencies": {
    "phaser": "^3.70.0"
  },
  "devDependencies": {
    "gh-pages": "^6.3.0",
    "vite": "^5.0.0"
  }
}
